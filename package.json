{
  "name": "token-tracker",
  "version": "0.1.0",
  "description": "Track AI token usage across sessions. CLI + Web dashboard. SQLite storage.",
  "license": "MIT",
  "bin": {
    "token-tracker": "./src/cli.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "minimist": "^1.2.8"
  },
  "scripts": {
    "start": "node src/cli.js",
    "serve": "node src/cli.js serve",
    "test": "node --test",
    "dashboard": "node src/cli.js serve --port 3000"
  }
}
